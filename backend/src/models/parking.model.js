class Parking {
    constructor(id, name, description, owner, spots) {
      (this.id = id),
        (this.name = name),
        (this.description = description),
        (this.owner = owner),
        (this.spots = spots)
    }
  }
  
export default Parking;